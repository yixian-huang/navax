# Brand static assets

Copied from repository root `logo-export/` (design export).

- `favicon.svg` + `favicon-*.png` — browser / PWA / apple-touch icons
- `mark-light.svg` — UI mark on light surfaces
- `mark-dark.svg` — UI mark on dark surfaces / dark wallpapers

Re-export: replace files here from `logo-export/` and keep names stable.
